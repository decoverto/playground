import 'reflect-metadata';

console.log('%c Decoverto playground', 'font-weight: bold; font-size: 50px;');
console.log(`Examples can be found in 'src/'. Import an example in index.ts or overwrite it with \
your own code.`)

import './src/inheritance';
